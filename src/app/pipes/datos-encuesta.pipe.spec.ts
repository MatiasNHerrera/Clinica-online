import { DatosEncuestaPipe } from './datos-encuesta.pipe';

describe('DatosEncuestaPipe', () => {
  it('create an instance', () => {
    const pipe = new DatosEncuestaPipe();
    expect(pipe).toBeTruthy();
  });
});
