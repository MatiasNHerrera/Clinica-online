import { HabilitadoPipePipe } from './habilitado-pipe.pipe';

describe('HabilitadoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HabilitadoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
