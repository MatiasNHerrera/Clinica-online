import { ListadoTurnosPacientePipe } from './listado-turnos-paciente.pipe';

describe('ListadoTurnosPacientePipe', () => {
  it('create an instance', () => {
    const pipe = new ListadoTurnosPacientePipe();
    expect(pipe).toBeTruthy();
  });
});
