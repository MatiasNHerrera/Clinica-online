import { DatosTurnoPipe } from './datos-turno.pipe';

describe('DatosTurnoPipe', () => {
  it('create an instance', () => {
    const pipe = new DatosTurnoPipe();
    expect(pipe).toBeTruthy();
  });
});
