import { TurnosDelDiaPipe } from './turnos-del-dia.pipe';

describe('TurnosDelDiaPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnosDelDiaPipe();
    expect(pipe).toBeTruthy();
  });
});
